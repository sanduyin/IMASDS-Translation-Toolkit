# IMASDS Translation Toolkit — 项目 Wiki

> 本文档说明工具链当前可用能力、已验证状态、文件约束与公开发布边界。
> 它不是逆向实验日志，也不收录原版游戏二进制、精确补丁地址、原始数据转储或可直接分发的游戏资产。
>
> **最后更新：2026-08-01**

---

## 目录

1. [项目概览](#1-项目概览)
2. [当前功能](#2-当前功能)
3. [文本与图像链路](#3-文本与图像链路)
4. [关键格式约束](#4-关键格式约束)
5. [编号与协作规则](#5-编号与协作规则)
6. [构建与验证](#6-构建与验证)
7. [公开发布边界](#7-公开发布边界)

---

## 1. 项目概览

IMASDS Translation Toolkit 是面向 *THE iDOLM@STER Dearly Stars* 的 Python 汉化构建工具链。它从使用者自行合法提取的原版 ROM 开始，完成文本、字库、歌词课程和多类 2D 图像的导出、编辑、回写与 ROM 重建。

### 当前状态

| 项目 | 状态 |
|------|------|
| 普通 NDS 模式 | 已完成构建与运行验证 |
| DSi/TWL 增强模式 | 已完成实机验证 |
| 3DS + Pico Loader 1.7.1 | 已验证进入汉化游戏；主菜单显示“扫描 QR 码”，可正常打开摄像头 |
| 歌词课程 | 10 首歌曲均已完成汉化资源构建；648 帧版本已运行验证 |
| SCN/TBL 文本 | 支持按原文件分别导出、校验与回写 |
| 字库 | 支持 LC10/LC12 动态构建 |
| 图像 | 支持 AGL/GLD、BG 与 OBJ 的导出和回写 |
| 自动化检查 | CI 已覆盖测试、类型检查与模块导入 |

上述结果是当前已验证基线，不代表任意烧录卡、启动器、模拟器版本或用户修改组合都必然兼容。修改底层资源后仍应重新执行完整构建和实机检查。

---

## 2. 当前功能

| 功能 | 说明 |
|------|------|
| 解包提取 | 从用户自备 ROM 提取内部封包、ARM9 与 Overlay，并处理所需压缩格式 |
| 导出文本 | 生成 SCN、TBL 与 ARM9/Overlay 翻译数据 |
| 导出图像 | 导出 AGL/GLD sprite、BG 及 OBJ 图像 |
| 构建字库 | 根据翻译内容生成 LC10/LC12 字库和字符映射 |
| 歌词课程汉化 | 统一生成歌词 BBQ、字符映射、AGL/GLD 字形资源及所需程序补丁 |
| 注入文本 | 将翻译写回 SCN、TBL、ARM9 与 Overlay |
| 回写图像 | 将修改后的图像写回 AGL/GLD、BG 与 OBJ |
| 打包生成 | 保留 NDS/DSi 必需结构并生成最终 ROM |
| 一键构建 | 依次执行字库、歌词、文本、图像和最终打包 |

交互菜单和命令行子命令共用同一套 Stage 实现。构建过程中遇到缺失资源、未知字符、结构异常或空间溢出时应停止，而不是静默回退到旧文件。

---

## 3. 文本与图像链路

### 3.1 普通文本

剧情、系统文本和邮件主要使用 SCN/TBL 数据，并通过 LC10/LC12 字库显示。工具链保持原文件与工作表的一一对应关系，避免把所有文本合并到单一工作表。

邮件包含标题、正文和回信等不同字段。导出与回写时必须保持字段对应、换行和长度限制；超限内容应在翻译表中明确提示。

### 3.2 歌词课程

歌词课程不使用普通对话字库，而是独立的字符映射与 AGL/GLD sprite 字形资源。歌词文本、候选字、字符映射和两套字形资源必须作为同一个版本构建和验证。

当前十首歌曲沿用既有文件编号和条目顺序。已有 glyph 编号视为稳定接口；新增字符只能向后追加，不能因为翻译增删而重排已经交付美工的图层。

### 3.3 图像

| 类型 | 用途 | 回写要点 |
|------|------|----------|
| AGL/GLD | 引擎 sprite 与歌词字形 | 保持 frame/footer 一一对应，尺寸和调色板必须合法 |
| BG | 背景图 | 保持 tile、调色板和画面映射关系 |
| OBJ | UI 与对象动画 | 保持 OAM/NCER 引用和共享 tile 一致 |

图像回写并不等于已经进入最终 ROM；最终打包阶段还会确认对应封包确实被替换并能回读。

---

## 4. 关键格式约束

### 4.1 PackEZ 封包

- 重建必须基于原封包的 entry 表，保留原有顺序、文件名、编号和压缩属性。
- 翻译侧看到的编号与原始 entry 之间存在兼容映射，不能只按数字位置盲目覆盖。
- 每个待替换文件必须同时通过文件名和编号校验。
- 重建后必须重新解析并逐项核对，防止首项丢失、错位或静默遗漏。

### 4.2 BBQ

- BBQ 是多 Section 容器；回写时必须保留 Section 顺序、非文本数据和文件尾结构。
- 字符串索引、共享指针和文本池之间的关系不能改变。
- 文本以零结尾；写入时必须准确更新容器声明的尺寸。
- 歌词事件引用的是文本条目。翻译可以改变显示文本，但不能随意删除、插入或重排事件所引用的条目。
- 候选字池属于玩法数据，不能当作普通歌词全部替换。

### 4.3 AGL/GLD

- AGL frame 与 GLD footer/像素资源按索引一一对应。
- 扩充字形时，两侧数量和索引必须同步更新。
- 既有 glyph ID 不回收、不重排；新字从当前锁定前缀之后追加。
- 构建完成后必须核对 BBQ → 字符映射 → AGL/GLD 的完整覆盖关系。

### 4.4 字库与编码

- 普通文本字库与歌词 sprite 字库是两条独立链路，不能混用映射。
- 构建字库时必须扫描正文、邮件正文与回信等全部翻译字段。
- 未映射字符应报告具体位置并停止，不能静默替换成问号。
- 映射生成必须稳定可复现，同一输入不应因运行顺序产生不同编号。

---

## 5. 编号与协作规则

1. 不修改既有资源文件名、四位编号或封包 entry 顺序。
2. 歌词课程继续保持当前十首歌曲的对应关系，不重新编号。
3. SCN/TBL 坚持“一份原文件对应一个 sheet”，便于多人分工和回写追踪。
4. 已交给美工的歌词 glyph ID 与 PSD 图层顺序视为锁定；新增字符只追加。
5. 不复用已分配编号来表示另一个字符。
6. 改动翻译表、图像和字库后，应由同一次构建重新生成所有相关产物，避免混用旧版 charmap、BBQ 或 AGL/GLD。
7. 合并代码修复时，不应顺带改动翻译内容、图片素材或资源编号。

---

## 6. 构建与验证

### 6.1 推荐顺序

1. 准备用户自行合法提取的原版 ROM、翻译表和自备字体。
2. 解包并导出文本、图像。
3. 完成翻译和美术修改。
4. 构建普通字库与歌词课程资源。
5. 注入文本并回写图像。
6. 执行最终打包和 ROM 审计。
7. 先在模拟器验证关键流程，再在目标实机验证。

### 6.2 构建门禁

最终打包前至少应确认：

- PackEZ 重建可完整回读，entry 数量、顺序与编号未变化；
- SCN/TBL BBQ 结构有效，文本回读与输入一致；
- OBJ、BG、AGL 和文本修改均已进入待打包封包；
- 歌词 BBQ 中的所有字符都有 charmap 与对应 AGL/GLD 字形；
- 所需程序补丁与当前输入版本匹配；
- NDS 与 DSi 区域的摘要、签名相关数据和加密区重建检查通过；
- 没有缺失文件、未知字符、尺寸溢出或被旧文件兜底的情况。

### 6.3 自动化检查

当前 CI 会运行单元测试、严格类型检查和模块导入检查，覆盖 PackEZ、BBQ 往返、CLI、DSi 构建与最终 ROM 审计等关键路径。CI 通过只能证明代码级回归检查通过，不能替代使用用户自有 ROM 的完整重建和实机验证。

---

## 7. 公开发布边界

本仓库公开发布的内容应限于工具源码、测试、通用格式说明和必要文档。以下内容不得提交或随 Release 分发：

- 原版或修改后的 ROM；
- 从 ROM 提取的 ARM9、ARM7、Overlay、BIN/IDX 或其他二进制；
- 原游戏的图片、字体、语音、音乐、脚本和其他受版权保护资产；
- 包含原游戏字节的预构建补丁产物或中间构建目录；
- 用户的翻译工作表、未获授权字体、密钥或本机隐私配置；
- 原始二进制地址转储、逐字节补丁清单、哈希白名单和仅用于内部复现的实验数据。

`game_data/`、`workspace/` 和 `build/` 均属于本地工作目录。它们可以由工具创建和使用，但其内容不应进入公开仓库。

公开文档只描述实现所需的高层结构和约束；精确偏移、补丁字节和从原版文件提取的数据应仅在合法持有者的本地环境中生成。发布工具不等于发布游戏内容，使用者仍须自行合法取得原版游戏并遵守所在地法律及相关许可。

---

如 Wiki 与实际程序行为不一致，应以当前源码、自动化测试和最新实机结果为准，并及时修正文档。